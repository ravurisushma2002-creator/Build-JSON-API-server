# 📦 JSON API Server (Users & Posts)

A simple **Node.js JSON API server** built using **core Node.js modules** (`http`, `fs`, `path`, `url`).  
This project reads data from local JSON files and exposes REST-style endpoints to fetch users, posts, and server status.

---

## 🚀 Features

- 📄 Reads data from `users.json` and `posts.json`
- 🔍 Fetch all users or a single user by ID
- 📝 Fetch all posts or a single post by ID
- 📊 Server status and performance metrics
- ⚡ No external libraries (pure Node.js)
- 📦 JSON-based API responses

---

## 🛠️ Tech Stack

- **Node.js**
- **HTTP module**
- **File System (fs)**
- **Path**
- **URL parser**

---

## 📁 Project Structure
